# 프로젝트 프로그래밍 기술 분석 보고서

## 📋 목차
1. 디자인 패턴
2. 아키텍처 패턴
3. C# 고급 기법
4. Unity 특화 기술
5. 적용 결과 및 효과

---

## 1️⃣ 디자인 패턴 (Design Patterns)

### 1.1 State Pattern (상태 패턴)
**위치:** `Assets/00_Scripts/Player/PlayerStateMachine.cs`, `Assets/00_Scripts/States/`

**구현 구조:**
- **인터페이스:** `IPlayerState` (OnEnter, OnUpdate, OnExit, CanTransitionTo)
- **추상 클래스:** `BasePlayerState` (공통 로직 구현)
- **구체 상태:** `IdleState`, `MoveState`, `AttackState`, `DodgeState`, `HitState`, `DeadState` 등
- **상태 관리자:** `PlayerStateMachine` (Dictionary<PlayerState, IPlayerState> 사용)

**사용 이유:**
- 플레이어의 복잡한 행동 상태(Idle, Move, Attack, Dodge 등)를 명확하게 분리
- 상태별 전이 규칙을 `CanTransitionTo()`로 관리하여 버그 예방
- 각 상태의 로직을 독립적으로 관리하여 확장성 향상

**적용 결과:**
- ✅ 상태 전이 로직이 명확하여 디버깅 용이
- ✅ 새로운 상태 추가가 쉬움 (예: ReloadState)
- ✅ 공격 중 회피, 공격 중 피격 등의 복잡한 전이도 안정적으로 처리

---

### 1.2 Strategy Pattern (전략 패턴)
**위치:** `Assets/00_Scripts/Weapon/IWeapon.cs`, `Assets/00_Scripts/Weapon/`

**구현 구조:**
- **인터페이스:** `IWeapon` (HandleInput, PerformAttack, GetCurrentAttackDamage 등)
- **구체 전략:** `Unarmed`, `LongSword`, `Pistol` (각각 다른 공격 로직)
- **컨텍스트:** `PlayerCombat` (IWeapon 참조를 통해 동적으로 무기 교체)

**사용 이유:**
- 무기별로 다른 공격 패턴(콤보, 데미지, 애니메이션)을 런타임에 교체 가능
- 새로운 무기 추가 시 기존 코드 수정 없이 확장 가능
- ScriptableObject(`WeaponData`)와 결합하여 데이터와 로직 분리

**적용 결과:**
- ✅ 맨손, 장검, 권총의 공격 로직이 완전히 분리되어 관리 용이
- ✅ 새로운 무기(예: MachineGun) 추가 시 `IWeapon` 구현만으로 추가 가능
- ✅ 무기 교체 시 `OnEquip/OnUnequip` 콜백으로 초기화 자동화

---

### 1.3 Factory Pattern (팩토리 패턴)
**위치:** `Assets/00_Scripts/Weapon/WeaponFactory.cs`

**구현 구조:**
```csharp
public static class WeaponFactory
{
    public static IWeapon Create(WeaponData data)
    {
        switch (data.weaponType)
        {
            case WeaponType.Melee: return new LongSword(data);
            case WeaponType.Ranged: return new Pistol(data);
            default: return new Unarmed(data);
        }
    }
}
```

**사용 이유:**
- WeaponData ScriptableObject를 기반으로 적절한 무기 인스턴스 생성
- 무기 생성 로직을 한 곳에 집중시켜 유지보수성 향상
- new 키워드를 직접 사용하지 않아 결합도 감소

**적용 결과:**
- ✅ 무기 생성 로직이 간단하고 명확
- ✅ WeaponManager에서 WeaponFactory.Create()만 호출하면 무기 교체 완료

---

### 1.4 Singleton Pattern (싱글톤 패턴)
**위치:** `Assets/00_Scripts/Managers/Managers.cs`, `Assets/00_Scripts/Effects/CameraShake.cs`

**구현 구조:**
```csharp
// Managers.cs
private static Managers _instance;
public static Managers Instance { get { return Initialize(); } }

// CameraShake.cs
public static CameraShake Instance { get; private set; }
```

**사용 이유:**
- **Managers:** InputManager 등 전역 매니저를 단일 인스턴스로 관리
- **CameraShake:** 카메라 쉐이크 효과를 어디서든 `CameraShake.Instance.ShakeLight()` 호출 가능

**적용 결과:**
- ✅ `Managers.InputManager`로 전역 접근 가능, null 체크 불필요
- ✅ `AttackHitbox`에서 직접 `CameraShake.Instance` 호출하여 간편한 피드백 구현

---

### 1.5 Template Method Pattern (템플릿 메서드 패턴)
**위치:** `Assets/00_Scripts/Player/BasePlayerState.cs`

**구현 구조:**
- **추상 클래스:** `BasePlayerState` (OnEnter에서 공통 초기화)
- **추상 메서드:** `OnUpdate()`, `OnExit()` (각 상태에서 구현)
- **가상 메서드:** `CanTransitionTo()` (기본 true, 필요시 오버라이드)

**사용 이유:**
- 모든 상태의 공통 로직(Player, Controller 참조 설정)을 한 곳에서 관리
- 각 상태는 고유 로직만 구현하면 됨

**적용 결과:**
- ✅ 상태 클래스들이 간결하고 일관된 구조 유지
- ✅ 공통 로직 변경 시 BasePlayerState만 수정하면 전체 적용

---

## 2️⃣ 아키텍처 패턴

### 2.1 Inheritance Hierarchy (상속 계층 구조)
**위치:** `Assets/00_Scripts/Enemy/`

**구현 구조:**
```
BaseEnemy (추상 클래스)
├── MeleeEnemy (근접 공격)
├── HandgunEnemy (권총 단발)
├── MachinegunEnemy (기관총 연사)
└── Enemy (HandgunEnemy 상속, 호환성)
```

**사용 이유:**
- 모든 Enemy의 공통 기능(체력, 데미지, 사망 처리)을 BaseEnemy에 집중
- 각 Enemy 타입은 고유 공격 로직만 구현 (`PerformAttack`, `GetAttackRange` 등)
- 코드 중복 제거 및 유지보수성 향상

**적용 결과:**
- ✅ 새로운 Enemy 타입 추가 시 BaseEnemy만 상속하면 기본 기능 자동 제공
- ✅ MyBT는 `BaseEnemy` 타입으로 모든 Enemy와 호환
- ✅ 공통 로직 변경 시 BaseEnemy만 수정하면 전체 적용

---

### 2.2 Behavior Tree (행동 트리)
**위치:** `Assets/00_Scripts/Enemy/MyBT.cs`, `MyNode.cs`, `MySelector.cs`, `MySequence.cs`

**구현 구조:**
- **노드 타입:** `MyNode` (추상), `MyLeaf` (리프), `MySelector` (OR), `MySequence` (AND)
- **트리 구조:** MySelector > MySequence > MyLeaf 형태로 복잡한 AI 로직 구현

**사용 이유:**
- Enemy AI의 복잡한 의사결정(사망 체크 → 스턴 체크 → 공격 범위 체크 → 공격 실행)을 트리 구조로 표현
- 조건부 실행과 순차 실행을 MySelector/MySequence로 명확히 구분
- 각 노드를 독립적으로 테스트 가능

**적용 결과:**
- ✅ Enemy 행동이 예측 가능하고 디버깅 용이
- ✅ AI 로직 추가 시 노드만 추가하면 됨 (예: "도망치기" 시퀀스 추가)
- ✅ 코드가 선언적이라 AI 설계 의도가 명확

---

### 2.3 Component Pattern (컴포넌트 패턴)
**위치:** 전체 프로젝트 (Unity 기본 패턴)

**구현 구조:**
- `Player`: PlayerController, PlayerCombat, PlayerHealth 등 여러 컴포넌트 조합
- `Enemy`: BaseEnemy, MyBT, Animator 등 독립적인 컴포넌트

**사용 이유:**
- Unity의 컴포넌트 시스템 활용으로 기능별 모듈화
- 컴포넌트 간 느슨한 결합으로 유연한 설계

**적용 결과:**
- ✅ 각 컴포넌트를 독립적으로 수정 가능
- ✅ 새로운 기능 추가 시 컴포넌트만 추가하면 됨

---

## 3️⃣ C# 고급 기법

### 3.1 Generic Method (제네릭 메서드)
**위치:** `Assets/00_Scripts/Managers/Managers.cs`

```csharp
private static T GetOrAddComponent<T>(GameObject target) where T : Component
{
    var component = target.GetComponent<T>();
    if (component == null)
        component = target.AddComponent<T>();
    return component;
}
```

**사용 이유:**
- 타입 안전성 보장 (형변환 불필요)
- 여러 컴포넌트 타입에 대해 동일한 로직 재사용

**적용 결과:**
- ✅ 컴포넌트 가져오기/추가 로직이 간결하고 타입 안전

---

### 3.2 Event System (이벤트 시스템)
**위치:** `Assets/00_Scripts/Managers/InputManager.cs`, `WeaponManager.cs`

**구현 구조:**
```csharp
public event Action<AttackType, bool> OnAttackPerformed;
public event Action<WeaponType> OnWeaponChanged;
```

**사용 이유:**
- 컴포넌트 간 느슨한 결합 (Publisher-Subscriber 패턴)
- 여러 구독자가 동시에 이벤트 처리 가능

**적용 결과:**
- ✅ InputManager의 입력을 여러 컴포넌트가 독립적으로 구독
- ✅ 무기 교체 시 WeaponManager가 이벤트 발행, PlayerCombat가 구독하여 자동 처리

---

### 3.3 Extension Methods (확장 메서드)
**사용 여부:** 프로젝트 내에서 직접 사용은 없으나, Unity API 활용

**참고:** Unity의 Transform, GameObject 등의 확장 메서드 활용

---

### 3.4 Property (프로퍼티)
**위치:** 전체 프로젝트

**사용 예:**
```csharp
public bool IsFacingRight => _facingRight; // 읽기 전용
public Vector3 FacingDirection => _facingRight ? Vector3.right : Vector3.left;
public Player Player => _player; // 캡슐화
```

**사용 이유:**
- 필드 직접 노출 대신 프로퍼티로 접근 제어
- 계산된 값도 프로퍼티로 표현 가능

**적용 결과:**
- ✅ 내부 필드 보호 및 유효성 검사 가능
- ✅ 계산된 값도 필드처럼 사용 가능 (예: FacingDirection)

---

## 4️⃣ Unity 특화 기술

### 4.1 Coroutine (코루틴)
**위치:** `Assets/00_Scripts/Weapon/Pistol.cs`, `Assets/00_Scripts/Enemy/HandgunEnemy.cs`

**사용 예:**
```csharp
private IEnumerator BurstFireCoroutine(Transform target)
{
    yield return new WaitForSeconds(shootDelay);
    for (int i = 0; i < burstCount; i++)
    {
        FireBullet(...);
        yield return new WaitForSeconds(burstDelay);
    }
}
```

**사용 이유:**
- 시간 기반 비동기 작업(연사 딜레이, 공격 후 경직)을 간단하게 구현
- Update() 루프 대신 코루틴으로 타이머 관리 불필요

**적용 결과:**
- ✅ 권총 3점사, Enemy 연사 등의 시간 기반 로직이 간결
- ✅ 성능 효율적 (매 프레임 체크 불필요)

---

### 4.2 ScriptableObject (스크립터블 오브젝트)
**위치:** `Assets/00_Scripts/ScriptableObject/WeaponData.cs`

**사용 이유:**
- 무기 데이터(데미지, 속도, 프리팹)를 코드와 분리
- 인스펙터에서 데이터 수정 가능
- 런타임 메모리 절약 (인스턴스 공유)

**적용 결과:**
- ✅ 무기 데이터 수정 시 코드 재컴파일 불필요
- ✅ 디자이너가 인스펙터에서 직접 데이터 조정 가능

---

### 4.3 Physics Layer & Collision Matrix (물리 레이어)
**위치:** `Assets/00_Scripts/States/DodgeState.cs`, 전체 프로젝트

**사용 예:**
```csharp
int invincibleLayer = LayerMask.NameToLayer("Invincible");
_player.gameObject.layer = invincibleLayer;
```

**사용 이유:**
- 회피 중 무적 상태를 레이어 변경으로 구현 (충돌 체크 불필요)
- EnemyProjectile과 Invincible 레이어 간 충돌 비활성화

**적용 결과:**
- ✅ 회피 시 투명하게 무적 처리
- ✅ Unity Physics Layer Collision Matrix로 간단하게 구현

---

### 4.4 Lerp (선형 보간)
**위치:** `Assets/00_Scripts/States/AttackState.cs`

**사용 예:**
```csharp
float t = 1f - (_attackMoveTimer / ATTACK_MOVE_DURATION);
velocity.x = Mathf.Lerp(_initialAttackVelocity, 0f, t);
```

**사용 이유:**
- 공격 이동 속도를 부드럽게 감속 (급정지 방지)
- 자연스러운 움직임 구현

**적용 결과:**
- ✅ 공격 후 이동이 자연스럽게 감속
- ✅ 플레이어 경험 향상

---

### 4.5 Input System (Unity Input System)
**위치:** `Assets/00_Scripts/Managers/InputManager.cs`, `PlayerInputAction.inputactions`

**사용 이유:**
- Unity의 새로운 Input System 사용으로 키보드/게임패드 등 다양한 입력 장치 지원
- 액션 기반 입력으로 코드 간결화

**적용 결과:**
- ✅ 입력 로직이 간결하고 확장 가능
- ✅ 게임패드 추가 시 inputactions만 수정하면 됨

---

## 5️⃣ 추가 고급 기법

### 5.1 Combo Buffer System (콤보 버퍼)
**위치:** `Assets/00_Scripts/States/AttackState.cs`

**구현 구조:**
- 애니메이션 완료 후 `COMBO_BUFFER_TIME`(0.5초) 동안 다음 입력 대기
- 버퍼 시간 내 입력 시 콤보 지속, 없으면 Idle 전환

**사용 이유:**
- 플레이어가 타이밍을 정확히 맞추지 않아도 콤보 입력 가능
- 게임 플레이 유연성 향상

**적용 결과:**
- ✅ 플레이어가 공격 입력을 미리 누르거나 조금 늦게 눌러도 콤보 유지
- ✅ 게임 난이도 완화

---

### 5.2 Direction Locking (방향 잠금)
**위치:** `Assets/00_Scripts/Player/PlayerController.cs`

**구현 구조:**
```csharp
private float _lockedDirection = 0f;

public void Move(float horizontalInput)
{
    if (_lockedDirection != 0f && Mathf.Sign(horizontalInput) == -_lockedDirection)
        return; // 반대 방향 입력 무시
    // ...
}
```

**사용 이유:**
- 공격/회피/장착 중 반대 방향 입력을 무시하여 애니메이션 깨짐 방지

**적용 결과:**
- ✅ 공격 중 뒤로 이동하여 애니메이션이 꼬이는 버그 방지
- ✅ 의도치 않은 방향 전환 방지

---

### 5.3 Resource Management (리소스 관리)
**위치:** `Assets/00_Scripts/Player/PlayerHealth.cs`

**구현 구조:**
- HP, SP(Stamina), Dodge Gauge를 자동 회복 시스템으로 관리
- Update()에서 시간 기반 회복 처리

**사용 이유:**
- 게임 밸런스를 시간 기반 회복으로 조절
- 플레이어가 전략적으로 리소스 사용

**적용 결과:**
- ✅ SP가 초당 0.5 회복되어 공격 빈도 조절
- ✅ Dodge Gauge가 초당 0.1 회복되어 회피 사용 제한

---

## 📊 적용 결과 요약

### ✅ 코드 품질 향상
- **모듈화:** 각 시스템이 독립적으로 동작하여 유지보수 용이
- **확장성:** 새로운 무기/Enemy 타입 추가가 간단
- **가독성:** 패턴 사용으로 코드 의도가 명확

### ✅ 개발 효율성 향상
- **디버깅 시간 단축:** State Pattern으로 상태 전이 추적 용이
- **기능 추가 속도 향상:** Factory Pattern으로 무기 추가 시간 단축
- **버그 감소:** Template Method Pattern으로 공통 로직 중복 제거

### ✅ 게임 플레이 품질 향상
- **반응성:** Combo Buffer System으로 입력 유연성 향상
- **몰입감:** CameraShake, HitStop 등 피드백 시스템으로 타격감 향상
- **밸런스:** Resource Management로 게임 난이도 조절 용이

---

---

## 🎮 게임 특징 분석

### 1️⃣ 전투 시스템의 깊이

#### 1.1 콤보 시스템 (Combo System)
**특징:**
- **3단 콤보 구조**: Z-Z-Z (약공격), X-X-X (강공격), Z-X-Z (혼합 콤보) 등 다양한 조합
- **콤보 버퍼 (0.5초)**: 애니메이션 종료 후 입력 여유 시간 제공 → 플레이어 친화적
- **무기별 차별화**: 맨손(빠른 콤보), 장검(강력한 콤보), 권총(단발/연사)

**게임 플레이 효과:**
- ✅ 숙련도에 따른 전투 스타일 차별화
- ✅ 입력 타이밍 유연성으로 진입 장벽 낮춤
- ✅ 무기별 전략 선택의 재미

---

#### 1.2 리소스 기반 전투 밸런스
**특징:**
- **HP (체력)**: 피격 시 감소
- **SP (스태미나)**: 공격 시 소모 (약공격 2.5, 강공격 5), 초당 0.5 회복
- **Dodge Gauge (회피 게이지)**: 회피 시 1회 소모 (최대 2회), 초당 0.1 회복

**게임 플레이 효과:**
- ✅ 무한 공격/회피 방지 → 전략적 리소스 관리 필요
- ✅ 자동 회복으로 무리한 소모전 방지
- ✅ 공격과 회피의 밸런스 강제

---

#### 1.3 회피 시스템 (Dodge System)
**특징:**
- **무적 프레임 (0.3초)**: 회피 시 `Invincible` 레이어로 변경하여 물리적으로 피격 무시
- **방향 잠금**: 회피 중 반대 방향 입력 무시
- **Kinematic 전환**: 회피 중 물리 충돌 무시

**게임 플레이 효과:**
- ✅ 액션 게임의 핵심 요소인 "회피" 메커닉 구현
- ✅ 패턴 학습과 타이밍 플레이 유도
- ✅ 스킬 기반 게임 플레이 가능

---

### 2️⃣ 타격감 및 피드백

#### 2.1 Camera Shake (카메라 쉐이크)
**적용 위치:** 플레이어 공격 시 (`AttackHitbox.cs`)
- **약공격**: 0.075 강도, 0.1초
- **중간**: 0.1 강도, 0.15초
- **강공격**: 0.1 강도, 0.25초

**게임 플레이 효과:**
- ✅ 타격감 극대화
- ✅ 데미지에 따른 시각적 피드백 차등화

---

#### 2.2 Hit Stop (히트 스톱)
**적용 위치:** 공격 적중 시 (`AttackHitbox.cs`)
- **TimeScale 조작**: 공격 적중 순간 시간을 잠깐 멈춤
- **강도별 차등**: 약공격(짧음), 강공격(김)

**게임 플레이 효과:**
- ✅ 타격의 무게감 전달
- ✅ 공격 적중 확인 용이

---

#### 2.3 Hit Effect (피격 이펙트)
**적용 위치:** 공격 적중 시 (`HitEffectManager.cs`)
- **Particle System**: 데미지에 따라 다른 이펙트 재생
- **Bullet Trail**: 총알 궤적 표시

**게임 플레이 효과:**
- ✅ 시각적 피드백으로 전투 몰입도 향상
- ✅ 공격 궤적 확인 용이

---

### 3️⃣ 다양한 무기 시스템

#### 3.1 무기 타입별 특징

| 무기 | 타입 | 공격 방식 | 특징 |
|------|------|-----------|------|
| **Unarmed (맨손)** | 근접 | 3단 콤보 (Z/X) | 빠르고 연속적, 기본 무기 |
| **LongSword (장검)** | 근접 | 3단 콤보 (Z/X) | 높은 데미지, 느린 속도 |
| **Pistol (권총)** | 원거리 | Z(단발) / X(3점사) | 안전 거리 유지, 연사 가능 |

**게임 플레이 효과:**
- ✅ 플레이어 선호도에 따른 무기 선택
- ✅ 상황별 최적 무기 전환 전략
- ✅ 다양한 플레이 스타일 지원

---

#### 3.2 무기 교체 시스템
**특징:**
- **E키로 줍기/버리기**: 필드에 있는 무기와 상호작용
- **실시간 교체**: 전투 중에도 무기 교체 가능
- **ScriptableObject 기반**: 인스펙터에서 무기 데이터 조정 가능

**게임 플레이 효과:**
- ✅ 전투 중 무기 변경으로 전략 다양화
- ✅ 디자이너가 밸런스 조정 용이

---

### 4️⃣ 지능형 AI 시스템

#### 4.1 Behavior Tree 기반 AI
**특징:**
- **우선순위 기반 행동**: 사망 → 스턴 → 원거리 공격 → 근접 공격 → 추격 → 대기
- **상태별 분기**: MySelector(OR), MySequence(AND) 노드로 복잡한 로직 구현
- **Attack Commit**: 공격 시작 후 중단 불가 (플레이어가 패턴 학습 가능)

**게임 플레이 효과:**
- ✅ 예측 가능한 AI로 패턴 학습 유도
- ✅ 공격 커밋으로 회피 타이밍 명확
- ✅ 다양한 Enemy 타입으로 전투 다양성 확보

---

#### 4.2 Enemy 타입별 특징

| Enemy 타입 | 공격 방식 | 범위 | 특징 |
|-----------|----------|------|------|
| **MeleeEnemy** | 근접 공격 | 1.5m | 플레이어에게 접근, 높은 데미지 |
| **HandgunEnemy** | 단발 사격 | 10m | 중거리 견제, 쿨다운 2초 |
| **MachinegunEnemy** | 5연사 | 12m | 원거리 압박, 연사 후 긴 쿨다운 |

**게임 플레이 효과:**
- ✅ Enemy 조합에 따라 전투 난이도 조절
- ✅ 각 Enemy 타입에 맞는 대응 전략 필요
- ✅ 근접/원거리 혼합 전투 유도

---

### 5️⃣ 기술적 완성도

#### 5.1 방향 잠금 (Direction Locking)
**특징:** 공격/회피/장착 중 반대 방향 입력 무시

**게임 플레이 효과:**
- ✅ 애니메이션 깨짐 방지
- ✅ 의도치 않은 방향 전환 방지
- ✅ 전투 조작감 향상

---

#### 5.2 Camera Occlusion (카메라 가림 처리)
**특징:** 
- **CameraOcclusionHandler**: 플레이어와 카메라 사이 오브젝트 투명화
- **CameraCollisionZoom**: 장애물 감지 시 카메라 줌인

**게임 플레이 효과:**
- ✅ 시야 확보 보장
- ✅ 플레이어 항상 추적 가능

---

#### 5.3 Smooth Attack Movement (부드러운 공격 이동)
**특징:**
- **Lerp 기반 감속**: 공격 후 이동 속도를 0.3초 동안 자연스럽게 감소
- **AttackMoveForce**: 공격 시 전진 이동 자동 적용

**게임 플레이 효과:**
- ✅ 공격 시 자연스러운 전진
- ✅ 급정지 방지로 몰입감 향상

---

## 🎯 핵심 게임 특징 요약

### 1. 깊이 있는 전투 시스템
- 콤보 시스템 + 리소스 관리 + 회피 메커닉의 조합
- 무기별 차별화된 공격 패턴
- 전략적 무기 교체

### 2. 강력한 타격감
- Camera Shake + Hit Stop + Hit Effect의 3중 피드백
- 공격 데미지에 따른 피드백 강도 차등화

### 3. 지능형 Enemy AI
- Behavior Tree 기반 우선순위 행동
- 다양한 Enemy 타입(근접/권총/기관총)
- 예측 가능한 Attack Commit 시스템

### 4. 높은 기술적 완성도
- 방향 잠금, 카메라 가림 처리, 부드러운 이동
- 디자인 패턴으로 확장 가능한 구조
- 버그 없는 안정적인 전투

---

## 🎯 결론

이 프로젝트는 **디자인 패턴, 아키텍처 패턴, C# 고급 기법, Unity 특화 기술**을 종합적으로 활용하여:
- **확장 가능하고 유지보수하기 쉬운 코드베이스** 구축
- **높은 코드 품질** 유지
- **뛰어난 게임 플레이 경험** 제공

특히 **State Pattern**과 **Strategy Pattern**의 조합으로 플레이어 행동과 무기 시스템을 완전히 분리하여, 코드 복잡도 증가에도 안정적으로 작동하는 시스템을 구축했습니다.

### 🌟 차별화 포인트

1. **콤보 버퍼 시스템**: 입력 유연성으로 진입 장벽 낮춤
2. **3중 피드백 시스템**: 타격감 극대화 (Shake + HitStop + Effect)
3. **Behavior Tree AI**: 복잡한 AI를 구조적으로 관리
4. **리소스 기반 밸런스**: 무한 공격/회피 방지로 전략 플레이 유도
5. **무기 다양성**: 3가지 무기의 차별화된 플레이 스타일

